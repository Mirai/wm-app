<h1>Orders</h1>

<table>
	<tr>
		<th>Name</th>
		<th></th>
		<th></th>
		<th></th>
	</tr>

<% @orders.each do |order| %>
	<tr>
		<td><%= order.name %></td>
		<td><%= link_to 'Show', order %></td>
		<td><%= link_to 'Edit', edit_order_path(order) %>
		<td><%= link_to 'Delete', order, :confirm => "Are you sure?", :method => :delete %>
	</tr>
<% end %>
</table>

<br />

<%= link_to 'New Order', new_order_path %>