<%= form_for(@model_type) do |f| %>
	<%= display_errors @model_type %>

	<div class="field">
		<%= f.label :name %><br />
		<%= f.text_field :name %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>