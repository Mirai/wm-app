<%= form_for @sub_type do |f| %>
	<%= display_errors @sub_types %>

	<div class="field">
		<%= f.label :name %><br />
		<%= f.text_field :name %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>