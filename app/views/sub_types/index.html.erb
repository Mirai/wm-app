<h1>Unit Types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>
    
<% @sub_types.each do |sub_type| %>
  <tr>
    <td><%= sub_type.name %></td>
    <td><%= link_to 'Edit', edit_sub_type_path(sub_type) %></td>
    <td><%= link_to 'Delete', sub_type, :confirm => "Are you sure?", :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Unit Type', new_sub_type_path %>